[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "swot-pixc-lab"
version = "0.4.0"
description = "Reproducible scientific workflows for SWOT L2 HR PIXC"
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
authors = [
  { name = "SWOT PIXC Lab contributors" },
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: GIS",
]
dependencies = [
  "earthaccess>=0.18,<1",
  "netCDF4>=1.7,<2",
  "numpy>=1.26,<3",
  "pandas>=2.2,<4",
  "pyproj>=3.6,<4",
  "shapely>=2.0,<3",
  "xarray>=2024.10,<2027",
]

[project.optional-dependencies]
visualization = [
  "matplotlib>=3.8,<4",
]
geo = [
  "geopandas>=1.0,<2",
]
notebook = [
  "ipykernel>=6.29",
  "nbformat>=5.10",
]
dev = [
  "matplotlib>=3.8,<4",
  "nbformat>=5.10",
  "pytest>=8",
  "pytest-cov>=5",
  "ruff>=0.8",
]

[tool.hatch.build.targets.wheel]
packages = ["src/swot_pixc_lab"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
  "integration: tests that contact NASA services and may require Earthdata credentials",
]

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
